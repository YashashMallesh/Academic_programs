Sieve of Erothosthenes:

ALGORITHM Sieve(n)
  //Generate prime numbers less than n.
  //I/P:A positive no. n>=2
  //O/P:Prime nos. <=n
  for i<-2 to n do
    a[i] <- i
  end for
  for j <- 2 to sqrt(n)
    if(a[p]!=0)
        i <- p*p
    while(i<=n)
        a[i] <- 0
        i <- i+p
    end while
 p <- p+1
end for
k <- 0
for i <- 2 to n do
  if(a[i]!=0)
    b[k] <- a[i]
  j <- j+1
      end if
    end for
Exit
